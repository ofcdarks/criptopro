version: "3.8"

services:
  app:
    build: .
    container_name: cryptoedge-pro
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - cryptoedge-data:/data

volumes:
  cryptoedge-data:
